{
  "name": "cp-contester",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "postinstall": "pnpm --filter @cp/db generate"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
